package main

import (
    "errors"
    "fmt"
)

func divmod(a, b int) (int, int, error) {
    if b == 0 {
        return 0, 0, errors.New("деление на ноль")
    }
    return a / b, a % b, nil
}

func main() {
    var a, b int
    fmt.Scan(&a, &b)

    q, r, err := divmod(a, b)
    if err != nil {
        fmt.Println(err.Error())
    } else {
        fmt.Printf("%d %d\n", q, r)
    }
}
